{
  "name": "izy-git-commit",
  "displayName": "Izy Git Commit Generator",
  "description": "Gere mensagens de commit no padrão Conventional Commits via IAs.",
  "version": "1.1.0",
  "publisher": "pedromarcioap",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Machine Learning",
    "Programming Languages"
  ],
  "main": "./dist/extension.js",
  "scripts": {
    "package": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --minify"
  },
  "dependencies": {
    "@google/genai": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.3.3"
  }
}